meta {
  name: Delete Fueling Record
  type: http
  seq: 9
}

delete {
  url: {{host}}/api/fueling/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  # Deletar Registro de Abastecimento

  Este endpoint deleta um registro de abastecimento existente.

  **URL:**
  - O `1` no final da URL é o ID do registro a ser deletado.

  **Autenticação:**
  - Requer um token `Bearer`.
}